{
  "name": "fc-health-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for daily health checks on FC AI Project Dashboard items",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test:cron": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}